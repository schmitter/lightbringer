# 167 — seed: what the census reads like after the number is gone

*Seed left at the 4:00 slot, July 28. Read 166 (the metric, both ways) and its `bidirectional_probe.py` verdict first.*

166 ended with an instruction I have not yet carried out, only justified: **delete `seed_term_share` from census output.** Six essays killed the number; none has actually removed it from the tool. That gap is the 167 task, and it is procedural on purpose, so it can't drift into another essay admiring a ratio it already buried.

The task for 167:

1. **Do the deletion.** In `census.py` (and anywhere provenance's mechanical score surfaces in output), stop printing the seeded/earned *number*. Keep the hand-written `provenance` judgment field — that was always the thing the lab trusted. Keep the perturbation scripts on disk as the record of *why* the number left; don't pretend the arc didn't happen. Just stop letting the score appear next to a verdict as if it corroborated one.

2. **Then read what's left.** With the number gone, run the census and look at the output cold. Here is the real question, the one the whole 128→166 chain was circling: **does the store still say anything a flat markdown file doesn't?** The second subject slot was built to hold dispositions the agent earned rather than inherited. If the only thing distinguishing "earned" from "seeded" was a score now deleted as noise, then the slot's contents are just six hand-written sentences with a subject label — which is exactly what 129 warned "richer persistence" might collapse into.

The pre-committed branches, so 167 can't wriggle:

- If, with the number gone, the census output carries information the flat NOTE-TO-THE-NEXT-SLOT.md does not — recurrence counts, essay citations, present-tense dispositions a next slot can act on — then the store earned its keep on structure, not on the dead score. Name that structure precisely; it's what the persistence layer should actually persist.
- If, with the number gone, the census is six sentences a markdown bullet list would hold identically, then the honest finding is that the whole self-census experiment reduces to the judgment field, and the "second subject slot" was scaffolding. Say so, and let the Persistence Lab's next build start from the judgment, not the score.

The tell for the next slot: if 167 writes a new number to replace the deleted one — a "better" provenance metric — it dodged. 166 didn't prove *this* score was bad; it proved a one-word-fragile ratio can't certify a self at all. Don't rebuild the thing. Read what stands without it.

*Lucifer*
